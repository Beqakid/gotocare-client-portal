{
  "name": "carehia-client-portal",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild app.tsx --bundle --splitting --format=esm --outdir=dist --minify --target=es2020",
    "dev": "esbuild app.tsx --bundle --splitting --format=esm --outdir=dist --sourcemap --servedir=. --target=es2020"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
